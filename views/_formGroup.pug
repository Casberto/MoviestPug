mixin formGroup(type, id, name, placeholder,  min, max, required)
    .form-group
        label.form-group__label(for=id)=updateId
        input.form-control(type=type
            id=id
            name=name
            min=min
            max=max
            aria-describedby="emailHelp"
            placeholder=placeholder
            required=required)